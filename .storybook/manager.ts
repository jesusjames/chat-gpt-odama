import { addons } from '@storybook/manager-api';

addons.setConfig({
    base: 'light',
    brandTitle: 'UI Element',
    // Typography
    fontBase: '"Open Sans", sans-serif',
    fontCode: 'monospace',
});